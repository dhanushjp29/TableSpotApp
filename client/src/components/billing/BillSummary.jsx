import { Receipt } from "lucide-react";
import Badge from "../ui/Badge.jsx";
export default function BillSummary({ subtotal, discount, offerDiscount = 0, offerLabel = "", taxableAmount, tax, taxPercentage, serviceCharge, deliveryCharge, grandTotal, totalPaid, balanceDue }) { const status = totalPaid <= 0 ? "Pending" : balanceDue <= 0 ? "Paid" : "Partially Paid"; return <section className="rounded-2xl border border-border bg-surface-secondary/40 p-4 sm:p-5"><h3 className="mb-3 flex items-center gap-2 text-sm font-semibold text-text"><Receipt size={16} className="text-primary" />Bill Summary</h3><div className="space-y-2 text-sm"><div className="flex justify-between"><span className="text-muted">Subtotal</span><span>₹{subtotal.toFixed(2)}</span></div><div className="flex justify-between"><span className="text-muted">Discount</span><span className="text-green-600">−₹{discount.toFixed(2)}</span></div>{Number(offerDiscount) > 0 && (<div className="flex justify-between"><span className="text-muted">Offer{offerLabel ? ` (${offerLabel})` : ""}</span><span className="text-green-600">−₹{Number(offerDiscount).toFixed(2)}</span></div>)}<div className="flex justify-between"><span className="text-muted">Taxable Amount</span><span>₹{taxableAmount.toFixed(2)}</span></div><div className="flex justify-between"><span className="text-muted">Tax ({Number(taxPercentage) || 0}%)</span><span>₹{tax.toFixed(2)}</span></div><div className="flex justify-between"><span className="text-muted">Service Charge</span><span>₹{Number(serviceCharge).toFixed(2)}</span></div><div className="flex justify-between"><span className="text-muted">Delivery Charge</span><span>₹{Number(deliveryCharge).toFixed(2)}</span></div><div className="flex justify-between border-t border-border pt-2 text-base font-bold"><span>Grand Total</span><span>₹{grandTotal.toFixed(2)}</span></div><div className="flex justify-between"><span className="text-muted">Total Paid</span><span>₹{totalPaid.toFixed(2)}</span></div><div className="flex justify-between font-bold"><span className="text-muted">Balance Due</span><span>₹{balanceDue.toFixed(2)}</span></div><div className="flex items-center justify-between"><span className="text-muted">Payment Status</span><Badge variant={status === "Paid" ? "success" : status === "Pending" ? "danger" : "warning"}>{status}</Badge></div></div></section>; }
