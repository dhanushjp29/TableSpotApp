function DashboardPlaceholder({ title }) {
  return (
    <div>
      <h1 className="text-2xl font-bold text-text">{title}</h1>
      <p className="mt-2 text-sm text-muted">This section is under construction.</p>
    </div>
  );
}

export default DashboardPlaceholder;
